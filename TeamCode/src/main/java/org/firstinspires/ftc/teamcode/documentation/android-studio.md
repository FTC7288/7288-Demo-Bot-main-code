# android - studio

- NOTE : ne pas mettre à jour gradle
- Tips:
    - Formattage auto du code
        - Settings/ Tools / Actions on save : reformat code + optimize import
            - permet de garder un code clair et lisible
    - rajout de la navigation + l'historique/show diff
        - right click sur la barre de titre / customize toolbar /
        - rechercher `show diff`, `show history`
        - rechercher `back` et `forward`

### Gemini

necessite un compte majeur
nathperol18@gmail.com

exemple de prompt:

```
Base toi sur l'intake et le driver pour creer un sous ensemble Feeder, composé d'un Servo
et qui aura un state interne (idle,pushing,retracting) ainsi qu'un state externe (idle, moving)
rappel toi qu'en ftc il faut gerer le boucle while (opModeIsActive()) lancé par le runOpMode durant laquelle il faudra faire les actions (bouger le servo, etc...)
```

